<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= movie.title %>
    </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>

<body>
    <div class="info">
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1>
        <%= movie.title %>
    </h1>
    <br>
    <p>RATING: <%= movie.rating %>
    </p>
    <p>AWARDS: <%= movie.awards %>
    </p>
    <p>LENGTH: <%= movie.length + ' min' %>
    </p>
    <p>RELEASE DATE: <%= movie.release_date %>
    </p>
    <br>
    <p>GENERO:</p>
    <% if (movie.genre) { %>
        <p><a href="/genres/detail/<%= movie.genre.id %>">
                <%= movie.genre.name %>
            </a></p>
        <% }else{ %>
            <p>Sin especificar</p>
            <% } %>
                <br>
                <hr>
                <h4>Actores</h4>
                <br>
                <ul>
                    <% movie.actors.forEach(actor=> { %>
                        <li><a href="/actors/detail/<%= actor.id %>">
                                <%= actor.first_name %> <b>
                                        <%= actor.last_name %>
                                    </b>
                            </a></li>
                        <% }) %>

                </ul>
                <br>
                <br>
                <section>
                    <!--Aquí debe colocar los botones de Modificar - Borrar y Listado de Películas-->
                    <p class="botones"><a class="botonAgregar" href="/movies/edit/<%= movie.id %>">Modificar Película</a>
                    <a class="botonBorrar" href="/movies/delete/<%= movie.id %>">Eliminar Película</a>
                    <a class="botonVolver" href="/movies">Peliculas</a></p>
                </section>
    </div>
</body>

</html>